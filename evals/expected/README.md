# Expected Eval Outputs

Store captured golden responses here after choosing a runtime and scoring process.

The baseline cases live in `../cases.jsonl`. Each expected response should preserve the output contract of its target skill and include every term listed in `must_include`.
